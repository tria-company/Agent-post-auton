---
phase: 02-agendamento-clickup-ghl
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/config/index.js
  - package.json
  - package-lock.json
  - .env.example
  - test/config.test.js
autonomous: false
requirements: [SCH-01, SCH-03, SCH-04, SCH-05, SCH-06, SCH-07]
user_setup:
  - service: clickup
    why: "Idempotência (D-01) e fonte de mídia (D-06) exigem dois custom fields cujos UUIDs só o usuário pode obter no workspace"
    dashboard_config:
      - task: "Criar custom field 'GHL Post ID' (tipo Text) na lista 'Agendamentos & Publicações' e copiar o UUID"
        location: "ClickUp → lista 901327135553 → Custom Fields → New field → Text"
      - task: "Confirmar/copiar o UUID do custom field 'Link do post' (tipo Text/URL, já existente)"
        location: "ClickUp → lista 901327135553 → Custom Fields → Link do post"
    env_vars:
      - name: CU_FIELD_GHL_POST_ID
        source: "UUID do novo campo 'GHL Post ID' criado pelo usuário"
      - name: CU_FIELD_LINK_DO_POST
        source: "UUID do campo 'Link do post' (zip no MinIO)"
      - name: CU_FIELD_FORMATO
        source: "UUID já conhecido: 24e0f126-589f-400c-a602-0e4abe19b809"
      - name: GHL_ACCOUNT_ID
        source: "Social Planner da conta auton.app — id que termina em _17841440215631995 (confirmar via ghl.listAccounts)"
      - name: STATUS_A_AGENDAR
        source: "Nome exato do status de detecção (default: 'a agendar')"
      - name: STATUS_AGENDADO
        source: "Nome exato do status de sucesso (default: 'agendado')"

must_haves:
  truths:
    - "O serviço falha no boot (fail-fast) se faltar qualquer uma das 6 novas variáveis obrigatórias do .env"
    - "config expõe CF_GHL_POST_ID, CF_LINK_DO_POST, CF_FORMATO, GHL_ACCOUNT_ID, STATUS_A_AGENDAR, STATUS_AGENDADO"
    - "adm-zip 0.5.17 está instalado e importável como default export ESM"
    - "Um upload real de 1 arquivo na media library do GHL + createPost agendado retorna response.post._id (A1/A2/A4/A7 validados empiricamente)"
  artifacts:
    - path: "src/config/index.js"
      provides: "Schema zod estendido + aliases CF_* das 6 novas vars"
      contains: "CU_FIELD_GHL_POST_ID"
    - path: "package.json"
      provides: "Dependência adm-zip"
      contains: "adm-zip"
    - path: "test/config.test.js"
      provides: "Asserções de fail-fast e presença das novas chaves no config"
      contains: "CF_GHL_POST_ID"
  key_links:
    - from: "src/config/index.js"
      to: "process.env"
      via: "EnvSchema.safeParse(process.env)"
      pattern: "CU_FIELD_GHL_POST_ID"
    - from: "src/config/index.js (Object.freeze)"
      to: "env.CU_FIELD_*"
      via: "alias CF_* = env.CU_FIELD_*"
      pattern: "CF_GHL_POST_ID:\\s*env\\.CU_FIELD_GHL_POST_ID"
---

<objective>
Estabelecer a fundação da Fase 2 e remover o bloqueio humano antes de qualquer slice de agendamento: o usuário cria os custom fields que ainda não existem no ClickUp e fornece os ids/nomes que só ele tem, o config passa a validar e expor as 6 novas variáveis (CFG-01), a dependência de unzip é instalada, e um smoke empírico de upload+createPost de UMA mídia valida cedo as suposições mais arriscadas da pesquisa (A1/A2/A4/A7) antes de investir na lógica completa do pipeline.

Purpose: Sem os UUIDs/account id no `.env`, nenhuma task elegível pode ser detectada (idempotência usa CF_GHL_POST_ID), nenhuma mídia pode ser resolvida (CF_LINK_DO_POST) e nenhum post pode ser criado (GHL_ACCOUNT_ID). E sem confirmar que o upload multipart nativo + createPost de fato funcionam, os planos 02/03 estariam construindo sobre suposições MEDIUM-confidence.
Output: config estendido + adm-zip instalado + smoke empírico verde, destravando os Waves 1 e 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agendamento-clickup-ghl/02-CONTEXT.md
@.planning/phases/02-agendamento-clickup-ghl/02-RESEARCH.md
@.planning/phases/02-agendamento-clickup-ghl/02-PATTERNS.md
@src/config/index.js
@src/clients/ghl.js
@test/config.test.js
</context>

## Phase Goal

**As a** gestor de conteúdo da Auton, **I want to** que uma task marcada `a agendar` com `Data de publicação` preenchida vire automaticamente um post agendado no GHL Social Planner do Instagram `auton.app` e volte como `agendado` com o id do post salvo, **so that** eu nunca precise copiar legenda, mídia e horário à mão entre o ClickUp e o GHL.

## Acceptance Criteria (Fase — este plano cobre a fatia de fundação)
- [ ] As 6 novas variáveis são lidas do `.env` com fail-fast (CFG-01) — nenhum id hardcoded
- [ ] adm-zip 0.5.17 instalado e importável em ESM
- [ ] Upload real de 1 arquivo + createPost agendado retorna `response.post._id` (suposições validadas)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender o config com as 6 novas variáveis da Fase 2 (CFG-01)</name>
  <files>src/config/index.js, .env.example, test/config.test.js</files>
  <read_first>
    - src/config/index.js (arquivo sendo modificado — padrão EnvSchema + Object.freeze + aliases CF_*)
    - .planning/phases/02-agendamento-clickup-ghl/02-PATTERNS.md (seção "src/config/index.js" — campos e aliases exatos a inserir)
    - test/config.test.js (análogo de teste de fail-fast/presença de chaves)
    - .planning/phases/02-agendamento-clickup-ghl/02-RESEARCH.md (tabela "Variáveis de config a adicionar")
  </read_first>
  <behavior>
    - Test 1: com `.env` completo, `config.CF_GHL_POST_ID`, `config.CF_LINK_DO_POST`, `config.CF_FORMATO`, `config.GHL_ACCOUNT_ID`, `config.STATUS_A_AGENDAR`, `config.STATUS_AGENDADO` estão definidos (não undefined)
    - Test 2: `STATUS_A_AGENDAR`/`STATUS_AGENDADO` assumem defaults `'a agendar'`/`'agendado'` quando ausentes
    - Test 3: faltar `CU_FIELD_GHL_POST_ID` (ou outra UUID obrigatória) produz fieldErrors no `safeParse` (fail-fast) — verificar via re-parse direto do EnvSchema exportável OU subprocesso com env incompleto retornando exit code 1
  </behavior>
  <action>
    Adicionar ao `EnvSchema` (zod) seis variáveis seguindo o padrão existente do arquivo: `CU_FIELD_GHL_POST_ID`, `CU_FIELD_LINK_DO_POST`, `CU_FIELD_FORMATO` como `z.string().uuid({ message: ... })` (obrigatórias); `GHL_ACCOUNT_ID` como `z.string().min(1, ...)` (obrigatória); `STATUS_A_AGENDAR` e `STATUS_AGENDADO` como `z.string().min(1).default('a agendar')` e `.default('agendado')` respectivamente. Implementa D-03 (CF_GHL_POST_ID), D-06 (CF_LINK_DO_POST), D-12/D-16/D-17 (Formato/status/account id como config). No `Object.freeze({...})` exportado, adicionar os aliases `CF_GHL_POST_ID: env.CU_FIELD_GHL_POST_ID`, `CF_LINK_DO_POST: env.CU_FIELD_LINK_DO_POST`, `CF_FORMATO: env.CU_FIELD_FORMATO`, e as chaves diretas `GHL_ACCOUNT_ID`, `STATUS_A_AGENDAR`, `STATUS_AGENDADO`. Atualizar o JSDoc `@type` do config e o mapeamento de comentário no topo do arquivo. NÃO alterar a lógica de `safeParse`/`process.exit(1)` — ela já cobre os campos novos. Atualizar `.env.example` adicionando as 6 chaves com comentários (deixar `CU_FIELD_FORMATO=24e0f126-589f-400c-a602-0e4abe19b809` preenchido; placeholders para os demais; NUNCA colocar valores reais de token/id sensíveis). Estender `test/config.test.js` com as 3 asserções do bloco behavior, seguindo o padrão de mock de env do arquivo existente.
  </action>
  <verify>
    <automated>node --test test/config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/config.test.js` passa com as novas asserções
    - As 6 chaves aparecem no objeto `config` exportado (CF_GHL_POST_ID, CF_LINK_DO_POST, CF_FORMATO, GHL_ACCOUNT_ID, STATUS_A_AGENDAR, STATUS_AGENDADO)
    - Faltar uma UUID obrigatória dispara fail-fast (fieldErrors / exit 1)
    - Nenhum id ou token hardcoded em src/config/index.js
  </acceptance_criteria>
  <done>As 6 variáveis novas são validadas (fail-fast) e expostas via config; teste verde.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar adm-zip 0.5.17 e confirmar import ESM</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (dependências atuais — confirmar ausência de adm-zip)
    - .planning/phases/02-agendamento-clickup-ghl/02-RESEARCH.md (seção "Nova dependência — unzip" + "Package Legitimacy Audit": adm-zip verdict OK, aprovado)
  </read_first>
  <action>
    Rodar `npm install adm-zip@0.5.17`. adm-zip tem verdict OK no Package Legitimacy Audit do RESEARCH.md (npm, ~13 anos, 17.8M downloads/semana, repo github.com/cthackers/adm-zip) — NÃO requer checkpoint de legitimidade (apenas pacotes [ASSUMED]/[SUS] requerem; adm-zip é [OK]/Aprovado). Confirmar que o import default ESM funciona em Node 24 sem `createRequire`: criar uma checagem ad-hoc que faz `import AdmZip from 'adm-zip'` e instancia `new AdmZip()` sem erro (pode ser um one-liner via `node --input-type=module -e`). NÃO criar arquivo de teste permanente para isto — a validação de uso real vem no Plano 02 (lib/zip.js).
  </action>
  <verify>
    <automated>node --input-type=module -e "import AdmZip from 'adm-zip'; new AdmZip(); console.log('adm-zip ESM OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `adm-zip` consta em `package.json` dependencies na versão 0.5.x
    - `import AdmZip from 'adm-zip'` + `new AdmZip()` executa sem erro em Node 24 ESM
  </acceptance_criteria>
  <done>adm-zip instalado e importável como default ESM.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint humano — criar custom fields no ClickUp, preencher .env, e validar upload+createPost real</name>
  <what-built>
    Tasks 1 e 2 já: estenderam o config para exigir as 6 novas variáveis (fail-fast) e instalaram adm-zip. O agente preparou um script de smoke empírico (`node src/scheduler/smoke-upload.mjs` OU um one-liner documentado abaixo) que faz UM upload real na media library do GHL e UM createPost agendado de teste, para validar empiricamente as suposições A1/A2/A4/A7 do RESEARCH.md ANTES de o Plano 02 construir o pipeline. O agente NÃO pode criar os custom fields nem descobrir os UUIDs/account id — isso exige acesso humano ao workspace ClickUp e ao Social Planner.
  </what-built>
  <how-to-verify>
    1. No ClickUp, abrir a lista "Agendamentos & Publicações" (id 901327135553) → Custom Fields → criar um campo Text chamado exatamente "GHL Post ID". Copiar o UUID do campo.
    2. Localizar o campo "Link do post" (Text/URL, já existente — distinto de "Link publicado") e copiar o UUID.
    3. Confirmar o `GHL_ACCOUNT_ID` da conta Instagram `auton.app`: rodar `node -e "import('./src/clients/ghl.js').then(m=>m.ghl.listAccounts()).then(r=>console.log(JSON.stringify(r,null,2)))"` e copiar o id da conta `auton.app` (deve terminar em `_17841440215631995` — A7). NÃO colar tokens nem a saída crua em chat.
    4. Confirmar os nomes EXATOS dos status no ClickUp: o de detecção (default 'a agendar') e o de sucesso (default 'agendado'). Se diferirem do default, anotar.
    5. Adicionar ao `.env` (gitignored — NUNCA commitar): `CU_FIELD_GHL_POST_ID=<uuid>`, `CU_FIELD_LINK_DO_POST=<uuid>`, `CU_FIELD_FORMATO=24e0f126-589f-400c-a602-0e4abe19b809`, `GHL_ACCOUNT_ID=<account id>`, e (se diferentes dos defaults) `STATUS_A_AGENDAR=...`, `STATUS_AGENDADO=...`.
    6. Rodar `node src/index.js SMOKE_ONLY=1` (ou `SMOKE_ONLY=1 node src/index.js`) — o boot deve passar pelo fail-fast do config sem erro (prova que as 6 vars estão presentes/válidas).
    7. SMOKE EMPÍRICO (validação A1/A2/A4/A7): rodar o smoke de upload+createPost que o agente preparou, com 1 imagem de teste pequena. Resultado esperado: o upload retorna `{url, fileId}` e o createPost retorna `response.post._id` não-vazio, e um post agendado de teste aparece no Social Planner da `auton.app`. Apagar/cancelar esse post de teste no GHL depois.
  </how-to-verify>
  <resume-signal>Responda "approved" com o status do smoke (upload OK? createPost retornou post._id?), ou descreva o erro (status HTTP, mensagem). Se o upload multipart falhar (A4) ou o createPost rejeitar a URL (A2), relate para o Plano 02 ajustar a estratégia antes de implementar o pipeline.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → processo Node | Segredos (tokens, UUIDs, account id) entram só via env; nenhum hardcode (CFG-01) |
| processo → GHL API | Upload multipart de arquivo binário + createPost cruzam para serviço externo |
| humano → repositório | Risco de o usuário commitar `.env` com segredos ao preencher os novos campos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `.env` preenchido pelo usuário | mitigate | `.env` é gitignored (Fase 1); o checkpoint instrui explicitamente NÃO commitar e NÃO colar tokens/saída crua de listAccounts em chat; `.env.example` recebe apenas placeholders (exceto o UUID público de Formato) |
| T-02-02 | Spoofing/Tampering | Config com id/token hardcoded | mitigate | Todas as 6 vars via zod fail-fast; teste assegura ausência de hardcode (CFG-01) |
| T-02-03 | Information Disclosure | Saída do smoke de upload/createPost | mitigate | logger já redige Authorization/token (Fase 1); checkpoint orienta não colar saída crua; smoke não loga a URL pre-signed |
| T-02-SC | Tampering | `npm install adm-zip` | accept | adm-zip verdict OK no Package Legitimacy Audit (Aprovado) — sem checkpoint de legitimidade exigido; pin em 0.5.17 |
</threat_model>

<verification>
- `node --test test/config.test.js` verde (fail-fast + presença das 6 chaves)
- `node --input-type=module -e "import AdmZip from 'adm-zip'; new AdmZip()"` sem erro
- Checkpoint humano aprovado com smoke de upload+createPost retornando `post._id`
- `grep -v '^#' src/config/index.js` não contém literais de UUID/token reais (exceto o UUID público de Formato, que pode estar só no .env.example)
</verification>

<success_criteria>
- As 6 variáveis novas validadas e expostas no config (CFG-01)
- adm-zip 0.5.17 instalado e importável em ESM
- Usuário criou "GHL Post ID", forneceu UUIDs/account id/status, e o smoke empírico confirmou upload+createPost (A1/A2/A4/A7)
- Waves 1 e 2 destravados
</success_criteria>

<output>
Create `.planning/phases/02-agendamento-clickup-ghl/02-01-SUMMARY.md` when done.
Registre no SUMMARY o resultado empírico do smoke (A1/A2/A4/A7: confirmado/ajuste necessário) e os nomes de status reais se diferirem dos defaults — o Plano 02 depende disso.
</output>
