---
phase: 03-webhooks-bidirecionais-clickup-ghl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/index.js
  - src/clients/ghl.js
  - scripts/smoke-ghl-getpost.js
  - test/verifySignature.test.js
  - test/dedupe.test.js
  - test/clickupHandler.test.js
autonomous: false
requirements: [SYNC-01]
user_setup:
  - service: ghl
    why: "O smoke OQ1 chama GET /social-media-posting/:loc/posts/:id com um post real para descobrir o shape; precisa do GHL_TOKEN já no .env (Phase 1/2)."
    env_vars:
      - name: GHL_TOKEN
        source: "Já presente no .env desde a Phase 1 — nenhuma nova credencial"

must_haves:
  truths:
    - "Um operador consegue rodar um smoke e ver, em log estruturado, os nomes REAIS dos campos do GET /posts/:id do GHL (status, IG media id, permalink, erro)"
    - "O config aceita e expõe WEBHOOK_PORT, CLICKUP_WEBHOOK_SECRET, POLL_INTERVAL_MS e STATUS_PUBLICADO via .env (fail-fast)"
    - "ghl.getPost(postId) retorna o JSON cru do post agendado do GHL"
    - "A suíte de testes contém 3 arquivos de teste novos que rodam (mesmo que RED) e fixam o contrato de HMAC, dedup e filtro de evento"
  artifacts:
    - path: "src/config/index.js"
      provides: "Campos Phase 3 no EnvSchema + objeto config congelado"
      contains: "CLICKUP_WEBHOOK_SECRET"
    - path: "src/clients/ghl.js"
      provides: "Método getPost(postId)"
      contains: "getPost"
    - path: "scripts/smoke-ghl-getpost.js"
      provides: "Smoke OQ1 — descobre shape do GET /posts/:id"
      min_lines: 20
    - path: "test/verifySignature.test.js"
      provides: "Contrato HMAC (TRIG-02) — RED até Plano 02"
      min_lines: 15
    - path: "test/dedupe.test.js"
      provides: "Contrato DedupeStore TTL (SYNC-06/TRIG-04) — RED até Plano 02"
      min_lines: 15
    - path: "test/clickupHandler.test.js"
      provides: "Contrato filtro de evento/status (TRIG-03) — RED até Plano 02"
      min_lines: 15
  key_links:
    - from: "src/clients/ghl.js"
      to: "GET /social-media-posting/{loc}/posts/{id}"
      via: "request('GET', ...)"
      pattern: "posts/\\$\\{postId\\}"
    - from: "scripts/smoke-ghl-getpost.js"
      to: "ghl.getPost"
      via: "import + chamada com post id real"
      pattern: "getPost"
---

<objective>
Estabelecer a fundação de configuração e o client do lado GHL para a Phase 3, e — antes de qualquer código de write-back — DESCOBRIR EMPIRICAMENTE o shape real do endpoint de status do post GHL (OQ1) e o nome exato do status `publicado` no ClickUp (OQ4). Também cria os scaffolds de teste (RED) que fixam os contratos de HMAC, dedup e filtro de evento que os Planos 02/03 vão satisfazer.

Purpose: O RESEARCH marcou OQ1/OQ4 como LOW-confidence (docs GHL JS-rendered/incompletas). A Phase 2 só não quebrou porque um smoke de Wave 0 corrigiu `userId` e `results.post._id` ANTES da implementação. Esta fase repete essa disciplina: o poller de write-back (Plano 03) será construído sobre fatos descobertos aqui, não sobre adivinhação de campos.

Output: config estendido (4 campos novos), `ghl.getPost`, um smoke script verificável por humano que imprime os nomes reais dos campos, e 3 arquivos de teste scaffold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-webhooks-bidirecionais-clickup-ghl/03-CONTEXT.md
@.planning/phases/03-webhooks-bidirecionais-clickup-ghl/03-RESEARCH.md
@.planning/phases/03-webhooks-bidirecionais-clickup-ghl/03-PATTERNS.md

# Código a estender (ler antes de editar — padrões exatos a seguir):
@src/config/index.js
@src/clients/ghl.js
@test/clients.test.js
</context>

<read_first>
- `src/config/index.js` — EnvSchema (linhas 27-60), bloco Phase 2 (linhas 45-54), objeto Object.freeze (linhas 99-122). Replicar EXATAMENTE o estilo (comentário de bloco + campos agrupados + alias no freeze + JSDoc @type).
- `src/clients/ghl.js` — método `listAccounts` (linhas 107-108) é o analog EXATO de `getPost`. Reusar a função `request('GET', ...)` interna; não criar wrapper novo.
- `test/clients.test.js` — helper `fakeResponse` (linhas 34-48) e padrão `t.mock.method(globalThis, 'fetch', ...)` + import dinâmico. Os scaffolds de teste copiam esse cabeçalho.
- RESEARCH.md secções OQ1, OQ4, Pattern 3, Assumptions A1/A2/A3 — os campos ESTIMADOS (`status`, `igMediaId`, `permalink`, `failureReason`) que o smoke precisa confirmar ou refutar.
- IMPORTANTE — divergência CONTEXT vs RESEARCH: o RESEARCH ainda cita smee.io / `SMEE_CHANNEL_ID` / `SKIP_SIGNATURE_VERIFY`. A CONTEXT.md D-08 foi REVISADA pós-pesquisa: ingress é Caddy direto, smee.io DESCARTADO. Portanto NÃO adicionar `SMEE_CHANNEL_ID` nem `SKIP_SIGNATURE_VERIFY` ao config. A verificação HMAC é sempre ativa (Caddy preserva o raw body).
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Estender config (Phase 3) e adicionar ghl.getPost</name>
  <files>src/config/index.js, src/clients/ghl.js</files>
  <read_first>
    - src/config/index.js linhas 45-60 (bloco Phase 2 no EnvSchema) e 112-121 (aliases Phase 2 no freeze)
    - src/clients/ghl.js linhas 107-108 (listAccounts como analog de getPost)
  </read_first>
  <action>
    Em `src/config/index.js`, adicionar ao `EnvSchema` um bloco "Phase 3 — servidor webhook + polling (CFG-01)" com exatamente 4 campos novos, seguindo o estilo zod do bloco Phase 2: `WEBHOOK_PORT` (z.string().default('3000').transform(Number)); `CLICKUP_WEBHOOK_SECRET` (z.string().min(1) com message 'CLICKUP_WEBHOOK_SECRET é obrigatório'); `POLL_INTERVAL_MS` (z.string().default('300000').transform(Number)); `STATUS_PUBLICADO` (z.string().min(1).default('publicado')). NÃO adicionar SMEE_CHANNEL_ID nem SKIP_SIGNATURE_VERIFY nem GHL_WEBHOOK_SECRET — D-08 revisado descartou smee.io; o ingress é Caddy direto e a verificação HMAC é sempre ativa. Adicionar os 4 campos correspondentes ao objeto `Object.freeze` (sem prefixo CU_FIELD_; passam direto de env.*) e estender o bloco JSDoc @type com os 4 campos. Atualizar o comentário-mapa do topo do arquivo listando os 4 novos.

    Em `src/clients/ghl.js`, adicionar ao objeto `ghl` exportado um método `getPost(postId)` espelhando `listAccounts`: `getPost: (postId) => request('GET', \`/social-media-posting/${config.GHL_LOCATION_ID}/posts/${postId}\`)`. Incluir JSDoc com a nota de que o shape do response é confirmado pelo smoke OQ1 (Task 2) e que campos de status/igMediaId/permalink são estimativas até lá. Não alterar mais nada em ghl.js.
  </action>
  <verify>
    <automated>cd "c:/Projetos Cursor/Agent-posts-auton" && node -e "import('./src/config/index.js').then(m=>{const c=m.config; if(!('CLICKUP_WEBHOOK_SECRET' in c)||!('STATUS_PUBLICADO' in c)||!('POLL_INTERVAL_MS' in c)||!('WEBHOOK_PORT' in c)) throw new Error('config faltando campos Phase 3'); if('SMEE_CHANNEL_ID' in c) throw new Error('SMEE_CHANNEL_ID nao deveria existir (D-08 revisado)'); console.log('OK config'); }).catch(e=>{console.error(e.message);process.exit(1);})"</automated>
  </verify>
  <done>config expõe WEBHOOK_PORT/CLICKUP_WEBHOOK_SECRET/POLL_INTERVAL_MS/STATUS_PUBLICADO e NÃO expõe SMEE_*; ghl.getPost existe e aponta para /posts/{id}.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke OQ1 — descobrir shape do GET /posts/:id (script verificável)</name>
  <files>scripts/smoke-ghl-getpost.js</files>
  <read_first>
    - src/clients/ghl.js (método getPost recém-criado)
    - src/lib/logger.js (withContext)
    - RESEARCH.md secção "Smoke Tests Obrigatórios" item 1 (SMOKE-GHL-GET-POST) e Assumptions A1/A2/A3
  </read_first>
  <action>
    Criar `scripts/smoke-ghl-getpost.js` (ESM, import 'dotenv/config' no topo). O script lê um post id da env `SMOKE_POST_ID` (default para o post agendado real da Phase 2: `6a39a0be892064b3bddd4ece`), chama `ghl.getPost(postId)` e imprime o JSON COMPLETO do response via `console.log(JSON.stringify(resp, null, 2))` para o operador inspecionar os nomes reais dos campos. Em seguida, derivar e logar (via withContext({module:'smoke'})) um resumo das chaves de topo descobertas: qual chave contém o status do post, se existe `igMediaId`/`instagramMediaId`/equivalente, se existe `permalink`/`postUrl`/equivalente, e qual chave traz a razão de falha. NUNCA logar o GHL_TOKEN nem o response cru via logger (use console.log só para o JSON de inspeção local; o resumo estruturado vai só com nomes de chaves, não valores sensíveis). O objetivo é registrar FATOS: o SUMMARY deste plano deve anotar os nomes reais dos campos para o Plano 03 consumir. Tratar erro de rede/4xx imprimindo a mensagem do AppError e saindo com código 1.
  </action>
  <verify>
    <automated>cd "c:/Projetos Cursor/Agent-posts-auton" && node --check scripts/smoke-ghl-getpost.js && echo "syntax OK"</automated>
    <human-check>Operador roda `node scripts/smoke-ghl-getpost.js` com .env válido e confirma no output o nome real do campo de status e dos campos de IG media id / permalink. Esses nomes são copiados para o SUMMARY (OQ1 resolvido).</human-check>
  </verify>
  <done>Script roda contra um post real e imprime o shape; os nomes reais dos campos de status/igMediaId/permalink/erro ficam documentados no SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Smoke OQ1 (shape do GET /posts/:id) executável e config Phase 3 carregando do .env.</what-built>
  <how-to-verify>
    1. Garantir `.env` com GHL_TOKEN/GHL_LOCATION_ID válidos e (provisoriamente) `CLICKUP_WEBHOOK_SECRET=placeholder` para o config não falhar no fail-fast.
    2. Rodar: `node scripts/smoke-ghl-getpost.js` (ou com `SMOKE_POST_ID=<id real>` se o post da Phase 2 não existir mais).
    3. Confirmar no output: (a) o nome REAL do campo que carrega o status do post; (b) os valores possíveis observados (ex.: `scheduled`/`published`/...); (c) os nomes reais de IG media id e permalink, se o post já estiver publicado; (d) onde fica a razão de falha.
    4. OQ4: no ClickUp (lista 901327135553), confirmar o nome EXATO do status de publicado (provavelmente `publicado`) e informar se difere do default.
    5. Reportar os nomes reais para serem gravados no SUMMARY.
  </how-to-verify>
  <resume-signal>Cole os nomes reais dos campos (status / igMediaId / permalink / erro) e o nome do status publicado, ou digite "confirmado defaults" se tudo bateu com a estimativa do RESEARCH.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Scaffold dos 3 arquivos de teste (RED) — contratos HMAC, dedup, filtro</name>
  <files>test/verifySignature.test.js, test/dedupe.test.js, test/clickupHandler.test.js</files>
  <read_first>
    - test/clients.test.js linhas 17-48 (cabeçalho + helper fakeResponse + padrão mock.method)
    - PATTERNS.md secção "Test files (3 new files)" — os corpos de teste exatos sugeridos
  </read_first>
  <action>
    Criar 3 arquivos de teste node:test que fixam os contratos que os Planos 02/03 implementarão. Eles importam de módulos que ainda não existem (`src/server/verifySignature.js`, `src/server/dedupe.js`, `src/server/routes/clickup.js`), portanto FICAM RED até o Plano 02 — isso é esperado e correto (test-first).

    `test/verifySignature.test.js`: importa `verifyClickUpSignature` de `../src/server/verifySignature.js`. Teste 1: assinatura HMAC-SHA256 hex válida (gerada inline com crypto.createHmac sobre um Buffer body + secret) retorna true. Teste 2: assinatura inválida retorna false (gate 401). Teste 3: header ausente/secret ausente retorna false.

    `test/dedupe.test.js`: importa `DedupeStore` de `../src/server/dedupe.js`. Teste 1: has(key) false para chave nova, true após set(key). Teste 2: expira após TTL (construir com ttl 10ms, set, await 20ms, has=false).

    `test/clickupHandler.test.js`: importa `handleClickUp` de `../src/server/routes/clickup.js`. Copiar o helper `fakeResponse` e o padrão de mock de `clickup.getTask`/`processTask`. Teste 1: evento != 'taskStatusUpdated' é no-op (responde 200, não chama processTask). Teste 2: status != STATUS_AGENDADO é no-op. Teste 3: HMAC inválido → res.writeHead chamado com 401 e processTask não chamado. Usar um objeto `res` fake com `writeHead`/`end` espionáveis (mock.fn) e um `req` fake com headers.

    NÃO implementar os módulos de produção aqui — apenas os testes. Marcar no topo de cada arquivo um comentário "RED até Plano 02 — test-first contract".
  </action>
  <verify>
    <automated>cd "c:/Projetos Cursor/Agent-posts-auton" && node --check test/verifySignature.test.js && node --check test/dedupe.test.js && node --check test/clickupHandler.test.js && echo "3 scaffolds com sintaxe valida"</automated>
  </verify>
  <done>Os 3 arquivos de teste existem, têm sintaxe válida e referenciam os módulos-alvo do Plano 02; ficam RED (módulos ainda não existem) — comportamento test-first esperado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → smoke script | O operador roda o smoke com o GHL_TOKEN do .env; nenhum input externo não confiável. |
| ghl.getPost → GHL API | Saída autenticada com Bearer token; resposta tratada como dado, não código. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | smoke-ghl-getpost.js imprime response cru | mitigate | Nunca logar GHL_TOKEN; o `console.log` do JSON é inspeção local efêmera; o resumo estruturado via logger contém só NOMES de chaves, não valores; redaction do pino já cobre authorization/token. |
| T-03-02 | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo neste plano (node:crypto/node:http são nativos; smee-client foi descartado em D-08). Sem superfície de supply-chain aqui. |
| T-03-03 | Information Disclosure | CLICKUP_WEBHOOK_SECRET no config | mitigate | Segredo lido só do .env (gitignored), nunca hardcoded; nunca passado a chamada de log (T-01-02). |
</threat_model>

<verification>
- `node -e` confirma os 4 campos Phase 3 no config e a ausência de SMEE_CHANNEL_ID.
- `node --check` em ghl.js (via import no verify da Task 1) e nos 3 scaffolds de teste.
- Smoke OQ1 executado por humano; nomes reais dos campos gravados no SUMMARY.
- `npm test` continua passando para os 67 testes existentes (os 3 novos ficam RED — esperado).
</verification>

<success_criteria>
- config expõe WEBHOOK_PORT, CLICKUP_WEBHOOK_SECRET, POLL_INTERVAL_MS, STATUS_PUBLICADO via .env (fail-fast) e NÃO expõe campos de smee.io.
- ghl.getPost(postId) implementado seguindo o analog listAccounts.
- Smoke OQ1 rodado; shape real do GET /posts/:id documentado (campos de status, igMediaId, permalink, razão de falha) no SUMMARY.
- OQ4 confirmado: nome exato do status publicado registrado.
- 3 scaffolds de teste criados (RED), fixando contratos de HMAC/dedup/filtro para o Plano 02.
</success_criteria>

<output>
Create `.planning/phases/03-webhooks-bidirecionais-clickup-ghl/03-01-SUMMARY.md` when done.
MUST record, in a "Findings (OQ1/OQ4)" section: o nome real do campo de status no GET /posts/:id, os nomes reais de igMediaId e permalink, o campo de razão de falha, e o nome exato do status publicado no ClickUp. O Plano 03 depende desses fatos.
</output>
